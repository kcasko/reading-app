// Shared theme values for the Reading App
const colors = {
  primary: '#4CAF50', // friendly green
  secondary: '#FFC107', // warm accent
  background: '#FFFFFF',
  text: '#111111',
  muted: '#9E9E9E',
  success: '#4CAF50',
  error: '#F44336',
};

const fonts = {
  regular: 'System',
  bold: 'System',
};

const spacing = {
  sm: 8,
  md: 16,
  lg: 24,
};

export default { colors, fonts, spacing };
// App theme configuration
// Colors, fonts, spacing used across the app
// Copilot: create simple theme object with primary colors and text styles
